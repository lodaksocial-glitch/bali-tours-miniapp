# 🚀 Быстрый старт

## За 5 минут до запуска

### 1. Установка (1 минута)

```bash
pip install -r requirements.txt
```

### 2. Конфигурация (2 минуты)

```bash
# Создайте .env
cp .env.example .env

# Сгенерируйте безопасный токен
python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Отредактируйте .env и вставьте:
# - Сгенерированный ADMIN_TOKEN
# - BOT_TOKEN от @BotFather
# - ADMIN_CHAT_ID (ваш Telegram ID)
```

### 3. Проверка (1 минута)

```bash
python3 check_security.py
```

Убедитесь, что нет критичных ошибок (❌).

### 4. Запуск (1 минута)

```bash
# Терминал 1: Flask сервер
export $(grep -v '^#' .env | xargs)
python3 server.py

# Терминал 2: Telegram бот
export $(grep -v '^#' .env | xargs)
python3 bot.py
```

### 5. Проверка работы

```bash
curl http://localhost:8080/api/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "database": "connected",
  "telegram": "configured"
}
```

## 🎉 Готово!

Откройте http://localhost:8080 в браузере.

## 📱 Настройка Telegram Mini App

1. Откройте @BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button → Configure Menu Button
5. URL: `https://your-app.onrender.com` (или ваш URL)
6. Отправьте `/start` вашему боту

## 🔒 Важно для продакшена

Перед деплоем обязательно:

1. ✅ Безопасный ADMIN_TOKEN (32+ символов)
2. ✅ Настроить ALLOWED_ORIGINS
3. ✅ Использовать HTTPS
4. ✅ Настроить Persistent Disk или PostgreSQL
5. ✅ Настроить автоматические бэкапы

Подробнее: [SECURITY.md](SECURITY.md)

## 📚 Дополнительная документация

- [INSTALL.md](INSTALL.md) - Подробная инструкция по установке
- [SECURITY.md](SECURITY.md) - Руководство по безопасности
- [IMPROVEMENTS.md](IMPROVEMENTS.md) - Список улучшений
- [CHANGELOG.md](CHANGELOG.md) - История изменений
- [README.md](README.md) - Полная документация

## 🆘 Проблемы?

### Flask not installed
```bash
pip install Flask==3.0.3
```

### Database is locked
- Убедитесь, что только один процесс использует БД
- Перезапустите приложение

### Invalid admin token
- Проверьте .env файл
- Убедитесь, что переменные загружены: `export $(grep -v '^#' .env | xargs)`

### Telegram бот не отвечает
- Проверьте BOT_TOKEN в .env
- Убедитесь, что WEB_APP_URL доступен по HTTPS
- Проверьте логи: `python3 bot.py`

## 💡 Полезные команды

```bash
# Проверка безопасности
python3 check_security.py

# Создание бэкапа
python3 backup_db.py

# Просмотр логов
tail -f server.log  # если настроено

# Обновление зависимостей
pip install --upgrade -r requirements.txt
```

## 🎯 Следующие шаги

1. Прочитайте [SECURITY.md](SECURITY.md)
2. Настройте автоматические бэкапы
3. Настройте мониторинг (опционально)
4. Деплой на Render/Railway
