# 🆓 Бесплатный деплой - Пошаговая инструкция

## 🎯 Стратегия: Render Free + Supabase PostgreSQL

**Что получите:**
- ✅ Полностью бесплатно
- ✅ База данных не теряется
- ✅ Автоматический HTTPS
- ✅ Автоматический деплой из GitHub

**Ограничения:**
- ⚠️ Сервис засыпает после 15 минут (холодный старт ~30 сек)
- ⚠️ 750 часов работы в месяц (достаточно для одного сервиса)

---

## 📋 Что понадобится (5 минут на регистрацию)

1. ✅ GitHub аккаунт - https://github.com/signup
2. ✅ Render аккаунт - https://dashboard.render.com/register
3. ✅ Supabase аккаунт - https://supabase.com/dashboard/sign-up
4. ✅ Telegram бот (токен от @BotFather)
5. ✅ Ваш Telegram ID (от @userinfobot)

---

## 🗄️ Часть 1: Настройка бесплатной PostgreSQL (10 минут)

### Шаг 1: Создание базы данных на Supabase

1. Откройте https://supabase.com/dashboard
2. Нажмите **New project**
3. Заполните:
   - **Name**: `bali-tours`
   - **Database Password**: Придумайте надежный пароль (сохраните!)
   - **Region**: Выберите ближайший (Europe West для Европы)
   - **Pricing Plan**: **Free** (0$/месяц, 500MB)
4. Нажмите **Create new project**
5. Подождите ~2 минуты пока создается база

### Шаг 2: Получение DATABASE_URL

1. В проекте откройте **Settings** (шестеренка слева внизу)
2. Выберите **Database**
3. Прокрутите до **Connection string**
4. Выберите **URI** (не Pooler!)
5. Скопируйте строку подключения:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
   ```
6. Замените `[YOUR-PASSWORD]` на ваш пароль из Шага 1

**Сохраните эту строку - она понадобится!**

### Альтернатива: Neon (если Supabase не подходит)

1. Откройте https://neon.tech
2. Sign up → Create project
3. Скопируйте Connection string
4. Бесплатно: 3GB, автоматическое засыпание

---

## 📦 Часть 2: Подготовка кода (5 минут)

### Шаг 1: Обновление requirements.txt

Добавьте PostgreSQL драйвер:

```bash
cd /Users/oleg/Downloads/Маршруты
echo "psycopg2-binary==2.9.9" >> requirements.txt
```

### Шаг 2: Инициализация Git

```bash
git init
git add .
git commit -m "Initial commit with PostgreSQL support"
```

### Шаг 3: Создание GitHub репозитория

1. Откройте https://github.com/new
2. Название: `bali-tours`
3. Visibility: **Private** (рекомендуется)
4. **НЕ** добавляйте README, .gitignore
5. Нажмите **Create repository**

### Шаг 4: Push в GitHub

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/bali-tours.git
git branch -M main
git push -u origin main
```

---

## 🚀 Часть 3: Деплой на Render (10 минут)

### Шаг 1: Создание Blueprint

1. Откройте https://dashboard.render.com
2. Нажмите **New +** → **Blueprint**
3. Подключите GitHub (если еще не подключен)
4. Выберите репозиторий `bali-tours`
5. Render найдет `render.yaml`
6. Нажмите **Apply**

Render создаст 2 сервиса:
- `bali-tours-web` (Flask API)
- `bali-tours-bot` (Telegram Bot)

### Шаг 2: Генерация ADMIN_TOKEN

Откройте терминал:
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте результат (например: `xK7mP9nQ2vR8sT4wU6yZ1aB3cD5eF7gH9iJ0kL2mN4o`)

### Шаг 3: Получение Telegram ID

1. Откройте Telegram
2. Найдите @userinfobot
3. Отправьте `/start`
4. Скопируйте ваш ID (например: `123456789`)

### Шаг 4: Настройка Web сервиса

1. В Render Dashboard откройте `bali-tours-web`
2. Перейдите в **Environment**
3. Найдите и измените/добавьте переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `DATABASE_URL` | `postgresql://postgres:...` | Строка из Supabase |
| `ADMIN_TOKEN` | Ваш сгенерированный токен | Из Шага 2 |
| `BOT_TOKEN` | `123456:ABC-DEF...` | От @BotFather |
| `ADMIN_CHAT_ID` | `123456789` | Ваш Telegram ID |
| `ALLOWED_ORIGINS` | `https://bali-tours-web.onrender.com,https://telegram.org` | CORS |

4. **Удалите** переменную `DB_PATH` (она не нужна для PostgreSQL)
5. Нажмите **Save Changes**

### Шаг 5: Настройка Bot сервиса

1. Откройте `bali-tours-bot`
2. Перейдите в **Environment**
3. Добавьте переменные:

| Key | Value |
|-----|-------|
| `BOT_TOKEN` | `123456:ABC-DEF...` |
| `ADMIN_CHAT_ID` | `123456789` |
| `WEB_APP_URL` | `https://bali-tours-web.onrender.com` |
| `BACKEND_URL` | `https://bali-tours-web.onrender.com` |

4. Нажмите **Save Changes**

### Шаг 6: Ожидание деплоя

Render автоматически задеплоит оба сервиса. Это займет ~5 минут.

Следите за логами:
- Откройте сервис → **Logs**
- Дождитесь сообщения: `Database initialized successfully`

---

## 🤖 Часть 4: Настройка Telegram Mini App (5 минут)

### Шаг 1: Настройка в BotFather

1. Откройте Telegram, найдите @BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. **Bot Settings** → **Menu Button** → **Configure Menu Button**
5. URL: `https://bali-tours-web.onrender.com`
6. Текст: `Создать маршрут`

### Шаг 2: Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "Создать маршрут"
4. Должен открыться Mini App

---

## ✅ Часть 5: Проверка работы (5 минут)

### Проверка 1: Health Check

Откройте в браузере:
```
https://bali-tours-web.onrender.com/api/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "timestamp": "2026-02-26T10:00:00Z",
  "database": "connected",
  "telegram": "configured"
}
```

### Проверка 2: Админка

Откройте:
```
https://bali-tours-web.onrender.com/admin
```

Введите ваш `ADMIN_TOKEN`.

### Проверка 3: Создание тестовой заявки

1. Откройте Mini App через Telegram бота
2. Создайте тестовый маршрут
3. Заполните форму клиента
4. Отправьте заявку
5. Проверьте, что:
   - ✅ Пришло уведомление в Telegram
   - ✅ Заявка появилась в админке
   - ✅ Данные сохранились

### Проверка 4: База данных в Supabase

1. Откройте Supabase Dashboard
2. Выберите ваш проект
3. **Table Editor** (слева)
4. Должны видеть таблицы: `leads`, `app_config`
5. В таблице `leads` должна быть ваша тестовая заявка

---

## 🎉 Готово! Что дальше?

### ✅ У вас теперь есть:
- Полностью бесплатное приложение
- База данных, которая не теряется
- Автоматический HTTPS
- Telegram Mini App

### 📊 Мониторинг

#### Render Dashboard:
- Логи: `bali-tours-web` → **Logs**
- Метрики: **Metrics** (CPU, Memory, Requests)

#### Supabase Dashboard:
- Использование БД: **Database** → **Usage**
- Таблицы: **Table Editor**
- SQL запросы: **SQL Editor**

### 🔄 Автоматические обновления

При каждом push в GitHub:
```bash
git add .
git commit -m "Update feature"
git push
```

Render автоматически задеплоит изменения.

---

## ⚠️ Важные ограничения бесплатного плана

### Render Free:
- ✅ 750 часов/месяц (достаточно для 1 сервиса 24/7)
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ Холодный старт ~30 секунд
- ⚠️ Только 1 сервис может работать 24/7

**Решение:** Используйте только Web сервис на Render, Bot запускайте локально или на другой платформе.

### Supabase Free:
- ✅ 500MB базы данных
- ✅ Автоматическое засыпание после 1 недели неактивности
- ✅ Автоматическое пробуждение при запросе

### Оптимизация для бесплатного плана:

#### Вариант А: Только Web на Render
```
Web сервис: Render (24/7)
Bot: Локально на вашем компьютере
База: Supabase
```

Запуск бота локально:
```bash
export BOT_TOKEN="ваш_токен"
export ADMIN_CHAT_ID="ваш_id"
export WEB_APP_URL="https://bali-tours-web.onrender.com"
export BACKEND_URL="https://bali-tours-web.onrender.com"
python3 bot.py
```

#### Вариант Б: Оба на Render (ограничено)
```
Web + Bot: Render
Ограничение: ~375 часов каждый = ~15 дней в месяц
```

---

## 🐛 Troubleshooting

### Проблема: "Database connection error"
**Решение:**
1. Проверьте `DATABASE_URL` в Render
2. Убедитесь, что пароль правильный
3. Проверьте, что Supabase проект активен
4. Попробуйте подключиться через SQL Editor в Supabase

### Проблема: "Service Unavailable" (503)
**Решение:**
- Сервис заснул. Подождите 30 секунд для пробуждения
- Это нормально для Free tier

### Проблема: "Too many connections"
**Решение:**
- Supabase Free ограничивает подключения
- Перезапустите Web сервис в Render
- Подождите несколько минут

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте логи `bali-tours-bot` в Render
2. Убедитесь, что сервис Running
3. Проверьте `BOT_TOKEN`
4. Перезапустите bot сервис

### Проблема: "Out of hours" на Render
**Решение:**
- Вы использовали 750 часов
- Варианты:
  1. Остановите Bot сервис, используйте только Web
  2. Запускайте Bot локально
  3. Upgrade до платного плана ($7/мес)

---

## 💡 Советы по экономии ресурсов

### 1. Используйте только Web сервис на Render
Bot можно запускать локально или на другой бесплатной платформе.

### 2. Оптимизируйте запросы к БД
Меньше запросов = меньше нагрузка на Supabase.

### 3. Мониторьте использование
- Render: Dashboard → Usage
- Supabase: Dashboard → Usage

### 4. Настройте алерты
Supabase может отправлять уведомления при достижении лимитов.

---

## 📈 Когда стоит перейти на платный план?

### Признаки, что пора платить:

1. ✅ У вас есть реальные клиенты
2. ✅ Холодный старт раздражает пользователей
3. ✅ Нужна стабильная работа 24/7
4. ✅ База данных > 500MB
5. ✅ Нужно больше 750 часов/месяц

### Варианты upgrade:

**Render Starter ($7/мес):**
- Без засыпания
- Persistent disk
- Приоритетная поддержка

**Railway ($5-10/мес):**
- Persistent storage
- Лучшая производительность
- Встроенный PostgreSQL

**Supabase Pro ($25/мес):**
- 8GB базы данных
- Больше подключений
- Автоматические бэкапы

---

## 🎯 Итоговая стоимость

### Текущая конфигурация:
```
Render Free:     $0/мес
Supabase Free:   $0/мес
─────────────────────
ИТОГО:           $0/мес
```

### Ограничения:
- 750 часов Render (достаточно для 1 сервиса)
- 500MB Supabase
- Холодный старт после 15 минут

### Когда нужно больше:
```
Render Starter:  $7/мес
Supabase Free:   $0/мес
─────────────────────
ИТОГО:           $7/мес
```

Или:
```
Railway:         $5-10/мес (все включено)
```

---

## 📞 Нужна помощь?

Если что-то не получается:
1. Проверьте логи в Render
2. Проверьте подключение к Supabase
3. Перечитайте инструкцию
4. Спросите меня!

**Удачи с деплоем! 🚀**
