# 🚀 Деплой на Render - Пошаговая инструкция

## ⚠️ Важно знать о Render Free Tier

### Ограничения:
- ❌ База данных в `/tmp` - **теряется при каждом рестарте**
- ❌ Сервис засыпает после 15 минут неактивности
- ❌ Холодный старт ~30 секунд
- ✅ 750 часов работы в месяц (достаточно для одного сервиса)

### Решения:
1. **Для тестирования**: Используйте как есть, делайте регулярные бэкапы
2. **Для продакшена**: Upgrade до Starter ($7/мес) с Persistent Disk
3. **Альтернатива**: Railway ($5-10/мес) с persistent storage из коробки

---

## 📋 Что вам понадобится

1. ✅ Аккаунт на GitHub
2. ✅ Аккаунт на Render (https://dashboard.render.com)
3. ✅ Telegram бот (токен от @BotFather)
4. ✅ Ваш Telegram ID (получите у @userinfobot)

---

## 🔧 Шаг 1: Подготовка кода

### 1.1 Инициализация Git (если еще не сделано)

```bash
cd /Users/oleg/Downloads/Маршруты

# Инициализация
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit with security improvements"
```

### 1.2 Создание GitHub репозитория

1. Откройте https://github.com/new
2. Название: `bali-tours` (или любое другое)
3. Описание: `Telegram Mini App for Bali Tours`
4. Visibility: **Private** (рекомендуется)
5. **НЕ** добавляйте README, .gitignore, license
6. Нажмите **Create repository**

### 1.3 Push в GitHub

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/bali-tours.git
git branch -M main
git push -u origin main
```

---

## 🌐 Шаг 2: Деплой на Render

### 2.1 Создание Blueprint

1. Откройте https://dashboard.render.com
2. Нажмите **New +** (в правом верхнем углу)
3. Выберите **Blueprint**
4. Нажмите **Connect GitHub** (если еще не подключен)
5. Выберите ваш репозиторий `bali-tours`
6. Render автоматически найдет `render.yaml`
7. Нажмите **Apply**

Render создаст 2 сервиса:
- `bali-tours-web` (Flask API)
- `bali-tours-bot` (Telegram Bot)

### 2.2 Генерация ADMIN_TOKEN

Откройте терминал и выполните:
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

Скопируйте результат (например: `xK7mP9nQ2vR8sT4wU6yZ1aB3cD5eF7gH9iJ0kL2mN4o`)

### 2.3 Получение Telegram ID

1. Откройте Telegram
2. Найдите бота @userinfobot
3. Отправьте `/start`
4. Скопируйте ваш ID (например: `123456789`)

---

## 🔐 Шаг 3: Настройка Environment Variables

### 3.1 Настройка Web сервиса

1. В Render Dashboard откройте `bali-tours-web`
2. Перейдите в **Environment**
3. Найдите `ADMIN_TOKEN` - он уже сгенерирован автоматически
4. Добавьте новые переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `BOT_TOKEN` | `123456:ABC-DEF...` | Токен от @BotFather |
| `ADMIN_CHAT_ID` | `123456789` | Ваш Telegram ID |
| `ALLOWED_ORIGINS` | `https://bali-tours-web.onrender.com,https://telegram.org` | CORS настройки |

5. Нажмите **Save Changes**

### 3.2 Настройка Bot сервиса

1. Откройте `bali-tours-bot`
2. Перейдите в **Environment**
3. Добавьте переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `BOT_TOKEN` | `123456:ABC-DEF...` | Тот же токен |
| `ADMIN_CHAT_ID` | `123456789` | Ваш Telegram ID |
| `WEB_APP_URL` | `https://bali-tours-web.onrender.com` | URL вашего web сервиса |
| `BACKEND_URL` | `https://bali-tours-web.onrender.com` | Тот же URL |

4. Нажмите **Save Changes**

---

## 🤖 Шаг 4: Настройка Telegram Mini App

### 4.1 Настройка в BotFather

1. Откройте Telegram, найдите @BotFather
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **Bot Settings**
5. Выберите **Menu Button**
6. Выберите **Configure Menu Button**
7. Введите URL: `https://bali-tours-web.onrender.com`
8. Введите текст кнопки: `Создать маршрут` (или любой другой)

### 4.2 Тестирование бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Должна появиться кнопка "Создать маршрут"
4. Нажмите на кнопку - откроется Mini App

---

## ✅ Шаг 5: Проверка работы

### 5.1 Health Check

Откройте в браузере:
```
https://bali-tours-web.onrender.com/api/health
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "timestamp": "2026-02-26T10:00:00Z",
  "database": "connected",
  "telegram": "configured"
}
```

### 5.2 Проверка админки

Откройте:
```
https://bali-tours-web.onrender.com/admin
```

Введите ваш `ADMIN_TOKEN` для доступа.

### 5.3 Проверка логов

В Render Dashboard:
1. Откройте `bali-tours-web`
2. Перейдите в **Logs**
3. Должны видеть:
   ```
   Database initialized successfully
   Bot is running and polling for updates...
   ```

---

## 🔄 Шаг 6: Настройка автоматических бэкапов

### Вариант А: GitHub Actions (рекомендуется)

1. Создайте файл `.github/workflows/backup.yml`:

```yaml
name: Database Backup

on:
  schedule:
    - cron: '0 3 * * *'  # Каждый день в 3:00 UTC
  workflow_dispatch:  # Ручной запуск

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Download leads data
        run: |
          curl -H "X-Admin-Token: ${{ secrets.ADMIN_TOKEN }}" \
               https://bali-tours-web.onrender.com/api/leads?limit=500 \
               > backup_$(date +%Y%m%d).json
      
      - name: Upload backup
        uses: actions/upload-artifact@v3
        with:
          name: database-backup
          path: backup_*.json
          retention-days: 30
```

2. Добавьте секрет в GitHub:
   - Settings → Secrets and variables → Actions
   - New repository secret
   - Name: `ADMIN_TOKEN`
   - Value: ваш ADMIN_TOKEN

### Вариант Б: Локальный скрипт

Запускайте локально каждый день:
```bash
# Скачать все заявки
curl -H "X-Admin-Token: ваш_токен" \
     https://bali-tours-web.onrender.com/api/leads?limit=500 \
     > backup_$(date +%Y%m%d).json
```

---

## 🚀 Шаг 7: Upgrade до Persistent Storage (опционально)

### Если нужна стабильная база данных:

1. В Render Dashboard откройте `bali-tours-web`
2. Перейдите в **Settings**
3. Измените Plan на **Starter** ($7/мес)
4. Добавьте **Persistent Disk**:
   - Name: `bali-tours-data`
   - Mount Path: `/var/data`
   - Size: 1 GB
5. Измените Environment Variable:
   - `DB_PATH` = `/var/data/leads.db`
6. Нажмите **Save Changes**

---

## 🐛 Troubleshooting

### Проблема: "Service Unavailable"
**Решение**: Сервис засыпает после 15 минут. Подождите 30 секунд для холодного старта.

### Проблема: "Invalid admin token"
**Решение**: 
1. Проверьте, что `ADMIN_TOKEN` одинаковый в обоих сервисах
2. Проверьте, что нет лишних пробелов
3. Перезапустите сервисы

### Проблема: "Database is locked"
**Решение**: Это нормально для SQLite. Подождите несколько секунд и повторите.

### Проблема: Бот не отвечает
**Решение**:
1. Проверьте логи `bali-tours-bot`
2. Убедитесь, что `BOT_TOKEN` правильный
3. Убедитесь, что `WEB_APP_URL` доступен
4. Перезапустите bot сервис

### Проблема: База данных пустая после рестарта
**Решение**: Это нормально для Free Tier. Варианты:
1. Upgrade до Starter с Persistent Disk
2. Используйте автоматические бэкапы
3. Переходите на Railway/Fly.io

---

## 📊 Мониторинг

### Проверка статуса сервисов

Render Dashboard показывает:
- ✅ Running - сервис работает
- 🟡 Building - идет деплой
- ❌ Failed - ошибка (смотрите логи)

### Важные метрики

В Render Dashboard → Metrics:
- CPU Usage
- Memory Usage
- Request Count
- Response Time

---

## 💰 Стоимость

### Free Tier:
- ✅ 750 часов/месяц
- ✅ Автоматический HTTPS
- ✅ Автоматический деплой из GitHub
- ❌ База данных ephemeral

### Starter ($7/мес):
- ✅ Все из Free
- ✅ Persistent Disk (1 GB включено)
- ✅ Приоритетная поддержка
- ✅ Без засыпания

---

## 🎯 Следующие шаги

1. ✅ Протестируйте создание заявки через Mini App
2. ✅ Проверьте, что уведомления приходят в Telegram
3. ✅ Настройте автоматические бэкапы
4. ✅ Добавьте свой домен (опционально)
5. ✅ Настройте мониторинг (опционально)

---

## 🆘 Нужна помощь?

- Render Docs: https://render.com/docs
- Render Community: https://community.render.com
- Telegram Support: @ваш_username (если настроили)

---

## 🔄 Альтернатива: Railway

Если Render не подходит, попробуйте Railway:
- Persistent storage из коробки
- $5 бесплатных кредитов
- Проще настройка

Инструкция: [DEPLOYMENT.md](DEPLOYMENT.md)
