# Telegram Mini App + Bot + Admin (Bali Tours)

Проект для авторских туров по Бали/Нуса Пенида/Ява:
- Mini App (конструктор маршрута + расчет цены)
- Backend API (Flask + SQLite)
- Админка заявок + редактор локаций (`/admin`)
- Telegram бот с кнопкой открытия Mini App

## Бизнес-логика цены

- Базовая цена: `1 200 000 IDR`
- В базу включено: `5 мест`
- Каждая дополнительная точка после 5: `+100 000 IDR`
- Входные билеты суммируются и добавляются к итогу

Формула:
`Итого = 1 200 000 + (доп.точки * 100 000) + сумма билетов`

## Структура

- `index.html` - интерфейс Mini App
- `styles.css` - стили Mini App
- `app.js` - логика маршрутов, калькулятор, отправка заявки
- `server.py` - Flask сервер, API, SQLite, админка
- `admin.html` - интерфейс управления заявками
- `bot.py` - Telegram бот (WebApp кнопка + прием данных)
- `render.yaml` - автодеплой на Render (web + worker)
- `Dockerfile` - контейнер для Railway/Render Docker
- `requirements.txt` - Python зависимости
- `.env.example` - пример переменных окружения

## API

- `POST /api/leads` - создать заявку
- `GET /api/leads` - список заявок (нужен `X-Admin-Token`)
- `PATCH /api/leads/<id>` - сменить статус (`new/contacted/booked/archived`)
- `GET /api/spots` - список локаций для Mini App и админки
- `POST /api/spots/bootstrap` - первичная инициализация локаций дефолтным каталогом
- `PUT /api/spots` - сохранить локации из админки (нужен `X-Admin-Token`)
- `GET /api/availability` - список занятых дат водителя
- `PUT /api/availability` - обновить занятые даты (нужен `X-Admin-Token`)
- `GET /api/analytics` - аналитика: заявки, средний чек, популярные точки/туры (нужен `X-Admin-Token`)
- `GET /api/health` - проверка сервиса

Примечание: при создании новой заявки с `customer.travel_date` эта дата автоматически добавляется в занятые (авто-блокировка).

## Быстрый деплой на Render

В проект уже добавлен `render.yaml`, поэтому можно сделать Blueprint deploy.

⚠️ **ВАЖНО:** Render Free Tier имеет ограничения:
- База данных в `/tmp` теряется при рестарте
- Сервис засыпает после 15 минут неактивности
- Для продакшена рекомендуется Railway или Render Starter

### Пошаговая инструкция:

**Подробная инструкция:** [RENDER_DEPLOY.md](RENDER_DEPLOY.md)

**Краткая версия:**
1. Push проекта в GitHub.
2. В Render: `New +` -> `Blueprint` -> выбрать репозиторий.
3. Render поднимет 2 сервиса:
   - `bali-tours-web` (Flask)
   - `bali-tours-bot` (Telegram worker)
4. В сервисе `bali-tours-bot` заполните секреты:
   - `BOT_TOKEN`
   - `ADMIN_CHAT_ID`
5. В сервисе `bali-tours-web` тоже заполните:
   - `BOT_TOKEN`
   - `ADMIN_CHAT_ID`
   Это нужно, чтобы backend отправлял новую заявку админу в Telegram сразу после сохранения.
   - `DB_PATH=/tmp/leads.db` (бесплатный режим без Persistent Disk)
   - `DB_PATH=/var/data/leads.db` (если подключен Persistent Disk)
6. В `@BotFather` запускайте бота и проверяйте `/start`.

### Точные значения URL для Render

Если используете имена из `render.yaml`, ставьте так:
- `WEB_APP_URL=https://bali-tours-web.onrender.com`
- `BACKEND_URL=https://bali-tours-web.onrender.com`

Админка:
- `https://bali-tours-web.onrender.com/admin`

## 🏆 Рекомендуемый деплой: Railway

**Почему Railway лучше:**
- ✅ Persistent storage (данные не теряются!)
- ✅ Простой деплой (10 минут)
- ✅ $5 бесплатных кредитов
- ✅ Стабильная работа
- ✅ ~$5-10/мес после кредитов

**Инструкция:** [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md)

## 📚 Полное руководство по деплою

- [RECOMMENDATION.md](RECOMMENDATION.md) - Какую платформу выбрать
- [RAILWAY_DEPLOY.md](RAILWAY_DEPLOY.md) - Деплой на Railway (рекомендуется)
- [RENDER_DEPLOY.md](RENDER_DEPLOY.md) - Деплой на Render (текущий)
- [DEPLOYMENT.md](DEPLOYMENT.md) - Сравнение всех платформ

## Деплой на Railway

Можно деплоить этот же репозиторий как 2 сервиса:
- `web` сервис: `APP_ROLE=web`
- `bot` сервис: `APP_ROLE=bot`

`Dockerfile` уже готов и запускает нужный процесс по `APP_ROLE`.

Минимальные переменные:
- Для `web`: `ADMIN_TOKEN`, `PORT` (Railway задает автоматически)
- Для `bot`: `BOT_TOKEN`, `ADMIN_CHAT_ID`, `WEB_APP_URL`, `BACKEND_URL`, `APP_ROLE=bot`

Для Railway `WEB_APP_URL` и `BACKEND_URL` должны быть равны публичному URL web-сервиса, например:
- `WEB_APP_URL=https://<your-web-service>.up.railway.app`
- `BACKEND_URL=https://<your-web-service>.up.railway.app`

## Локальный запуск

```bash
cd /Users/oleg/Downloads/Маршруты
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Отредактируйте .env и установите безопасный ADMIN_TOKEN
export $(grep -v '^#' .env | xargs)
python3 server.py
```

В другом терминале:

```bash
cd /Users/oleg/Downloads/Маршруты
source .venv/bin/activate
export $(grep -v '^#' .env | xargs)
python3 bot.py
```

## Безопасность

⚠️ **КРИТИЧНО:** Перед деплоем прочитайте [SECURITY.md](SECURITY.md)

Основные требования:
1. Сгенерируйте безопасный ADMIN_TOKEN:
   ```bash
   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
   ```
2. Настройте ALLOWED_ORIGINS для CORS
3. Используйте HTTPS в продакшене
4. Настройте автоматические бэкапы базы данных

## Новые возможности

### Rate Limiting
- Защита от злоупотреблений API
- 200 запросов/час, 50 запросов/минуту на IP
- 10 запросов/минуту на создание заявок

### Улучшенное логирование
- Все критичные операции логируются
- Отслеживание попыток несанкционированного доступа
- Детальные ошибки для отладки

### Валидация данных
- Санитизация всех входных данных
- Защита от SQL injection
- Проверка форматов телефонов, дат, URL

### Health Check
Проверка состояния приложения:
```bash
curl https://your-app.onrender.com/api/health
```

Ответ:
```json
{
  "ok": true,
  "timestamp": "2026-02-26T10:00:00Z",
  "database": "connected",
  "telegram": "configured"
}
```

## Настройка в BotFather

1. Создайте бота через `@BotFather`.
2. Получите токен и вставьте в `BOT_TOKEN`.
3. Убедитесь, что `WEB_APP_URL` - HTTPS ссылка на ваш Mini App.
4. Запустите бота и отправьте `/start`.

## Важно про базу

Сейчас используется SQLite (`leads.db`).
- На локалке это удобно.
- На облаке без постоянного диска база может очищаться после рестартов.
- Для продакшена лучше подключить Postgres (следующий шаг).

### Миграция на PostgreSQL (рекомендуется для продакшена)

1. Установите зависимость:
```bash
pip install psycopg2-binary
```

2. Добавьте переменную окружения:
```bash
DATABASE_URL=postgresql://user:password@host:port/database
```

3. Приложение автоматически переключится на PostgreSQL

### Автоматические бэкапы

Создание бэкапа базы данных:
```bash
python3 backup_db.py
```

Настройка автоматического бэкапа (cron):
```bash
# Ежедневно в 3:00
0 3 * * * cd /path/to/app && python3 backup_db.py
```

Environment variables:
- `BACKUP_DIR` - директория для бэкапов (по умолчанию: ./backups)
- `BACKUP_RETENTION_DAYS` - сколько дней хранить старые бэкапы (по умолчанию: 30)
